export const metadata = {
  title: "Organization Setup | Maintix",
};

import { Organization } from "@/features/setup";

export default function OrganizationSetupPage() {
  return <Organization />;
}
